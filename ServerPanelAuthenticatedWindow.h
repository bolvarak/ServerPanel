#ifndef SERVERPANELAUTHENTICATEDWINDOW_H

///////////////////////////////////////////////////////////////////////////////
/// Definitions //////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////

#define SERVERPANELAUTHENTICATEDWINDOW_H

///////////////////////////////////////////////////////////////////////////////
/// Headers //////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////

#include <QWidget>

///////////////////////////////////////////////////////////////////////////////
/// Namespace ////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////

namespace Ui {
    // Set our window class into the namespace
    class ServerPanelAuthenticatedWindow;
}

///////////////////////////////////////////////////////////////////////////////
/// ServerPanelAuthenticatedWindow Definition ////////////////////////////////
/////////////////////////////////////////////////////////////////////////////

class ServerPanelAuthenticatedWindow : public QWidget {
    // Make sure we have a QObject
    Q_OBJECT
// Public
public:
    // Singleton
    static ServerPanelAuthenticatedWindow* Instance();
    // Destructor
    ~ServerPanelAuthenticatedWindow();
    
private:
    // Constructor
    explicit ServerPanelAuthenticatedWindow(QWidget* cParent = 0);
    // Properties
    static ServerPanelAuthenticatedWindow* mInstance;      // Singleton Instance
    Ui::ServerPanelAuthenticatedWindow*    mUserInterface; // User Interface
};

#endif // SERVERPANELAUTHENTICATEDWINDOW_H
